import React from 'react';

const Search = () => {

    useEffect(() => {
        window.scrollTo(0, 0);
    },[])
    
    return (
        <main>
            <h2>SEARCH</h2>
        </main>
    );
};

export default Search;