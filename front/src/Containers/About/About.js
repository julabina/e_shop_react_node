import React from 'react';

const About = () => {

    useEffect(() => {
        window.scrollTo(0, 0);
    },[])
    
    return (
        <main>
            
        </main>
    );
};

export default About;